import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.danish.smartqueue',
  appName: 'Smart Queue Management System',
  webDir: 'build'
};

export default config;
